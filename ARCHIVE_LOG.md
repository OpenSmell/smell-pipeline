# Archive Log

## 2026-05-14: v0 Post-hoc Decoder Archived

### Moved to archive/
- `src/train_decoder.py` → `archive/v0_posthoc_decoder/train_decoder.py`
- `src/extract_latents.py` → `archive/v0_posthoc_decoder/extract_latents.py`

### Deleted
- `models/decoder.pth` — trained decoder with R² ≈ -0.08 (broken)
- `models/decoder_validation.json` — CV results from broken decoder
- `data/substance_latents.csv` — generated by the failed extract step
- `demo/app.py` — lookup-table demo that depended on the broken decoder

### Why
The post-hoc linear decoder from classification CNN latent space → chemoprint failed (R² near zero). The CNN latent space (trained for 50-class discrimination) does not linearly encode chemical properties. Replaced by the encoder approach (MAE + contrastive) in `opensmell/encoder`.
